const AddVocabulary = ({wordsList}) => {
return <div className="wordBox">
     {wordsList.map((entry, index) => (
        <div className="">
        
        </div>
     ))}
</div>

}

export default AddVocabulary;